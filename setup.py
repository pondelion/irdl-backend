from distutils.core import setup
from setuptools import setup, find_packages


setup(
  name         = 'irdl',
  description  = 'irdl',
  url          = 'https://github.com/pondelion/irdl-backend',
  packages     = ['irdl'],
)
